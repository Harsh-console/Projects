#include<bits/stdc++.h>
using namespace std;

int main(){
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int tt;
    while(tt--){
        string s;
        cin>>s;
        int n = s.length();
        for(int i = 0; i < n; ++i){
            for(int j = i; j < n; ++j){
                string sCopy = s;
                
            }
        }
    }
    return 0;
}
